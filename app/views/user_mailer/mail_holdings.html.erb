<% if @stocks > 0 || @stocks_yesterday > 0 %>

<%= render :template => 'user_mailer/_fund' %>

<%= render :template => 'user_mailer/_perf' %>

<% else %>

<p>You have no stock holdings.  Please visit our site and <%= link_to 'add a holding', new_holding_url %>.</p>

<% end %>

<p>This message was automagically generated at <%= Time.current %></p>
